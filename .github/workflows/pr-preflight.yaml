name: "Pull Request Preflight Check"

on:
  pull_request_target:
    branches:
      ["main"]
    types:
      ["opened", "synchronize", "reopened"]

# Deny all permissions by default
permissions: {}

jobs:
  test-pr:
    name: "Test if pull request is valid"
    if: ${{ github.event.action != 'closed' }}
    runs-on: ubuntu-22.04
    outputs:
      is_valid: ${{ steps.check-pr.outputs.VALID }}
    permissions:
      pull-requests: write
    steps:
      - name: "Get Invalid Hashes File"
        id: hash
        run: |
          echo "json<<EOF
          $(curl -sL https://files.carpentries.org/invalid-hashes.json)
          EOF" >> $GITHUB_OUTPUT
      - name: "Check PR"
        id: check-pr
        uses: carpentries/actions/check-valid-pr@24898f898388e129d2bbc1f2d8634d7f79cc356b  # v1.0.0
        with:
          pr: ${{ github.event.number }}
          invalid: ${{ fromJSON(steps.hash.outputs.json)[github.repository] }}
          fail_on_error: true
      - name: "Comment result of validation"
        id: comment-diff
        if: ${{ always() }}
        uses: carpentries/actions/comment-diff@24898f898388e129d2bbc1f2d8634d7f79cc356b  # v1.0.0
        with:
          pr: ${{ github.event.number }}
          body: ${{ steps.check-pr.outputs.MSG }}
